# URL base da API do backend Fluig
# Em produção (embedded no Fluig), deixe vazio ou configure o path correto
VITE_API_URL=/query

# Exemplo para desenvolvimento local (porta do servidor Fluig/WildFly):
# VITE_API_URL=http://localhost:8080/totvs-query/api/query
